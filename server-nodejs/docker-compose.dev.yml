version: "3.8"

services:
  api:
    build:
      context: .
      target: development  
    volumes:
      - ./:/usr/src/app
      - /usr/src/app/node_modules 
    command: npm run dev 
    ports:
      - 5000:5000 
         
