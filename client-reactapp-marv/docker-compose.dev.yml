version: "3.8"

services:
  react:
    labels:
      - "com.docker.compose.container-number=1"
    build:
      context: .
      target: development  
    volumes:
      - ./:/usr/src/app
      - /usr/src/app/node_modules 
    command: npm run dev 
    ports:
      - 6000:6000 
